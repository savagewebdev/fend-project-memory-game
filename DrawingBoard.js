function startingValues () {
	
}


//const Zero = {
//	begin: 1,
//	set commence(x) {
//		this.begin = x - x; // Come up with math to come out as zero.
//	}
//}
//
//Zero.commence = 0;
//Zero.begin;
//
//const Three = {
//	begin: 6,
//	set commence(x) {
//		this.begin = x - x; // Come up with math to come out as three.
//	}
//}

Three.commence = 3;
Three.begin;

//function.call(obj); Calls a function and applies  "this" to come up with the values from an object.

var obj = {
	zero: 0, three: 3;
};

// Three points

// Something that contains the starting values.
// Something which converts the values into something usable.
// The incrementization or calling of those values.